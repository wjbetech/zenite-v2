### Deployable environment file (used by Render)
### Replace the placeholder values in your Render service or secrets dashboard.
### Do NOT commit real secrets to the repo. Use Render's environment variables UI.

# Database connection (Render Postgres connection string)
# DO NOT store live secrets in the repository. Set this value in your
# host provider (Render/Vercel) or paste it into a local-only file such
# as `.env.production.local` when testing locally.
# Example placeholder – replace in your host secrets UI:
DATABASE_URL="postgresql://<REPLACE_WITH_RENDER_DATABASE_URL>"

# Clerk (Auth) keys
# NOTE: Do NOT commit real secrets to the repository. Set these in your
# hosting provider's environment variables (Render, Vercel) or in a
# local, git-ignored file such as `.env.production.local` for local
# production testing. Example file: `.env.production.local.example`.
# CLERK_SECRET_KEY=sk_live_<REDACTED>
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_<REDACTED>
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# Environment marker
NEXT_PUBLIC_ENV=production

# Optional: deployment tokens (if you use them in CI/deploy workflows)
# VERCEL_TOKEN or other deploy tokens should be set in the deploy service, not committed.
# VERCEL_TOKEN="<VERCEL_TOKEN>"