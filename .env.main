### Default environment (development & testing)
# This file is intended as the single canonical "main" template for
# development and testing. It contains non-sensitive placeholders
# and defaults. Developers may copy or override values locally, but
# do NOT commit live secrets into the repository.

# Local development database (example)
DATABASE_URL="postgresql://postgres:password@localhost:5432/zenite_dev?schema=public"

# Clerk (Auth) publishable key for local testing (use test key)
# If you need to run without editing this file, replace <YOUR_TEST_PK>
# with a test publishable key from Clerk. Prefer developer-specific
# local overrides for secrets.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_<YOUR_TEST_PK>
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

NEXT_PUBLIC_ENV=development
