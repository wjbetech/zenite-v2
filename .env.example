## Example .env template. Copy to .env.local for local development and to .env for deploy (or set secrets in your provider).

# Database example (replace in your environment):
DATABASE_URL="postgresql://postgres:password@localhost:5432/zenite_dev?schema=public"

# Clerk example (do NOT commit real keys):
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="<CLERK_PUBLISHABLE_KEY>"
CLERK_SECRET_KEY="<CLERK_SECRET_KEY>"

# NEXT_PUBLIC_ENV can be 'development' or 'production'
NEXT_PUBLIC_ENV=development
