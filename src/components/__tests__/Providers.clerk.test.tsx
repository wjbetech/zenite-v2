// This test file was intentionally turned into a no-op. The clerk key
// safety behavior is covered by src/components/__tests__/clerkKeyGuard.test.ts

describe('placeholder', () => {
	test('noop', () => {
		expect(true).toBe(true);
	});
});
